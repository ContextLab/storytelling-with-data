# data-visualization-mcp
Server that exposes datasets and visualization tools via Model Context Protocol (MCP). Allows clients and LLMs to interact with the server to upload data, specify visualizations, and retrieve results as part of a contextual workflow.
